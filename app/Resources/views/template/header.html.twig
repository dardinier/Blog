<nav class="navbar navbar-expand-lg navbar-dark">
    <a class="navbar-brand" href="{{ path('homepage') }}">Mon blog</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-between" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('articles') }}">Articles</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('categories') }}">Catégories</a>
            </li>
        </ul>

        <div class="navbar-nav">
            {% if app.user %}
                <div class="nav-item nav-link user__name">
                    Connecté en tant que {{ app.user.username }}
                </div>
                <a href="/logout" class="btn btn-outline-danger">Déconnexion</a>
            {% else %}
                <a href="/login" class="btn btn-outline-success">Se connecter</a>
            {% endif %}
        </div>
    </div>
</nav>
